{
  "scripts": {
    "pw-run-smoke": "npx playwright test --config smoke.config.ts --headed",
    "pw-run-smoke-ci": "npx playwright test --config smoke.config.ts",
    "pw-run-regr": "npx playwright test --config regression.config.ts --workers=3"
  },
  "dependencies": {
    "@playwright/test": "^1.50.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "dotenv": "^16.4.5",
    "typescript": "^5.3.3"
  }
}
